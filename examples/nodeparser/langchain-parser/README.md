# Langchain Node Parser Example

This example demonstrates how to use the `LangchainNodeParser` to bridge a Langchain text splitter to LlamaIndex node parsers.

## Prerequisites

Ensure you have the module installed and dependencies downloaded.

## Running the Example

```bash
go run main.go
```

## Expected Output

You should see 3 parsed nodes extracted using the mock Langchain text splitter.
