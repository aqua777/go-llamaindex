# Unstructured Element Node Parser Example

This example demonstrates how to use the `UnstructuredElementNodeParser` to parse unstructured text into paragraph element nodes.

## Prerequisites

Ensure you have the module installed and dependencies downloaded.

## Running the Example

```bash
go run main.go
```

## Expected Output

You should see 3 parsed paragraph nodes extracted from the plain text.
